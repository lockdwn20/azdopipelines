trigger: none   # manual run for PoC

pool:
  vmImage: 'windows-latest'

steps:
- task: PowerShell@2
  inputs:
    targetType: 'filePath'
    filePath: 'scripts/Sync-WikiToTheHive.ps1'
  env:
    System.AccessToken: $(System.AccessToken)
    TheHiveApiKey: $(TheHiveApiKey)